
  # Voice-Assisted Memory App UI

  This is a code bundle for Voice-Assisted Memory App UI. The original project is available at https://www.figma.com/design/GA3O0WelbHVDqb60oixfSG/Voice-Assisted-Memory-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  